{
  "name": "<%= kata.slug %>-with-phpunit",
  "version": "<%= version %>",
  "description": "PHPUnit-based TDD kata for <%= kata.slug %>",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "<%= name %>",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "gulp": "^3.8.11",
    "gulp-phpunit": "^0.8.4"
  }
}
